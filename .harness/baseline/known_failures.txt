# Known pre-existing test failures (baseline) — SINGLE SOURCE OF TRUTH
#
# These tests fail on lead/main HEAD WITHOUT any local changes. Two consumers
# rely on this file:
#   1. ENV-BYPASS authorization (CLAUDE.md Rule 7.1, authorize_env_bypass.sh)
#      distinguishes "pre-existing baseline failures" from "new regressions".
#   2. The deterministic regression guard (tools/harness/regression_verdict.sh,
#      pipeline Step 2.7) set-differences a feature's Generator gate result
#      against this list — a failing test NOT listed here is a NEW regression
#      and blocks the commit.
#
# Provenance: DERIVED FROM A CLEAN `cargo test --workspace` ON lead/main HEAD
# 16f2b93a (2026-06-21, with the 2-5a work stashed). The workspace run fail-fast
# stopped after the `harness_resource_scarcity` binary failed; the 25 binaries
# that sort after it (harness_s16_*/settlement*/show_*/starvation/stockpile/
# harness_t7_*) were then run explicitly with `--no-fail-fast` to complete
# coverage. Full result across all 92 test binaries: exactly ONE failing test
# (below); every other test passes.
#
# This SUPERSEDES the void pre-V7 shelter-ring / c1ee01c0 "27 − 11 = 16" lineage
# (discarded by the V7-RESET clean-slate ae0a01f6, 2026-05-04). The previous
# 16-entry list referenced tests that NO LONGER EXIST after the V7 reset
# (0 `tests::harness_*` occurrences; no `harness_body_damage_api` binary) — it
# was never re-derived post-reset. This list is the actual current failure set,
# re-verified, not inherited.
#
# baseline_test_failures.txt holds only the COUNT and MUST equal the number of
# entries below (currently 1). Keep them in sync.
#
# Format: one full test path per line, exactly as cargo prints it
#   (module-qualified for unit tests: `mod::fn`, `tests::fn`;
#    bare top-level fn for integration-test binaries: `harness_x_y`).
# The regression guard reads the first whitespace token per non-comment line;
# trailing text on the line is a human-readable note. Comments (#) and blank
# lines ignored. Duplicates silently deduplicated.
#
# Update protocol:
#   - When a baseline failure is FIXED, remove its line in the same commit AND
#     decrement baseline_test_failures.txt to match.
#   - When a NEW failure is observed and judged unrelated to the current change,
#     document it here (separate commit + follow-up issue) AND increment the
#     count file. Never silently expand this file to make a noisy commit pass.

harness_scarcity_a15_determinism_of_scarcity_run   determinism divergence at tick 2251/5000 — REPRODUCIBLE (3/3 runs diverge at the identical tick with the identical dead-set; not flaky). Pre-existing: "Fix D" did not restore full-horizon cross-run determinism. BACKLOG — underlying sim non-determinism, out of scope for the baseline/guard reconciliation; whether/when to fix is a separate decision. Panic at crates/sim-test/tests/harness_resource_scarcity.rs:1227.
