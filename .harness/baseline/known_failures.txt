# Known pre-existing test failures (baseline) — SINGLE SOURCE OF TRUTH
#
# These tests fail on lead/main HEAD WITHOUT any local changes. Two consumers
# rely on this file:
#   1. ENV-BYPASS authorization (CLAUDE.md Rule 7.1, authorize_env_bypass.sh)
#      distinguishes "pre-existing baseline failures" from "new regressions".
#   2. The deterministic regression guard (tools/harness/regression_verdict.sh,
#      pipeline Step 2.7) set-differences a feature's Generator gate result
#      against this list — a failing test NOT listed here is a NEW regression
#      and blocks the commit.
#
# CURRENT STATE: ZERO known baseline failures. The full `cargo test --workspace`
# on lead/main HEAD passes with NO failures. Any failing test is therefore a NEW
# regression and MUST block — the regression guard set-differences against an
# empty list, so an empty `failing` set yields CLEAN and any failure is flagged.
#
# baseline_test_failures.txt holds only the COUNT and MUST equal the number of
# entries below (currently 0). Keep them in sync.
#
# History:
#   - The sole prior entry, `harness_scarcity_a15_determinism_of_scarcity_run`,
#     was FIXED (commit chain fcbab050 → a15-fix). Root cause: AgentDecisionSystem
#     iterated the `settlement_migrant_tag` HashSet to `world.insert_one(SettlementMigrant)`,
#     applying a STRUCTURAL component insert in RandomState-seed order. That
#     permuted the hecs SettlementMigrant-archetype dense order → seed-dependent
#     query iteration order (first observable at tick 9) → a contended gather
#     split diverged (inventory, tick 489) → an agent need value diverged (the
#     a15 tick-2251 fingerprint divergence). Fixed by sorting the migrant
#     entities by stable AgentId before the insert
#     (agent_decision.rs ~L1582). a15 now passes the full 5000-tick per-tick
#     lockstep across independently-seeded engines (verified 3/3 runs).
#   - This file SUPERSEDES the void pre-V7 shelter-ring / c1ee01c0 "27 − 11 = 16"
#     lineage (discarded by the V7-RESET clean-slate ae0a01f6, 2026-05-04).
#
# Format: one full test path per line, exactly as cargo prints it
#   (module-qualified for unit tests: `mod::fn`, `tests::fn`;
#    bare top-level fn for integration-test binaries: `harness_x_y`).
# The regression guard reads the first whitespace token per non-comment line;
# trailing text on the line is a human-readable note. Comments (#) and blank
# lines ignored. Duplicates silently deduplicated.
#
# Update protocol:
#   - When a baseline failure is FIXED, remove its line in the same commit AND
#     decrement baseline_test_failures.txt to match.
#   - When a NEW failure is observed and judged unrelated to the current change,
#     document it here (separate commit + follow-up issue) AND increment the
#     count file. Never silently expand this file to make a noisy commit pass.

# (no entries — zero known baseline failures)
